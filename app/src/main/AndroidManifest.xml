<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cs3398.sportsapp">

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".View.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.MainActivity" />
        <activity android:name=".View.HomeActivity" />
        <activity android:name=".View.BracketActivity" />
        <activity android:name=".View.NewBracketActivity" />
        <activity android:name=".View.SearchActivity" />
        <activity android:name=".View.ProfileActivity" />
        <activity android:name=".View.FirstRoundActivity" />
        <activity android:name=".View.SecondRoundActivity" />
        <activity android:name=".View.ThirdRoundActivity" />
        <activity android:name=".View.WinnerActivity" />
        <activity android:name=".View.CurrentBracketsActivity" />
        <activity android:name=".View.CompletedBracketsActivity" />
        <activity android:name=".View.CreateAccountActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDGb_NtUFqrIEjYmuMbWadLa-Es0d-WBrE" />

        <activity
            android:name=".View.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".View.FriendsListActivity" />
        <activity
            android:name=".View.SearchUsernameActivity"
            android:label="@string/title_activity_search_username"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>